{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
 */
#}

{% if row._entity.field_news_media_type[0].entity.name.value and row._entity.field_news_media_type[0].entity.name.value|lower == 'twitter' %}
  {% if fields.field_twitter_post_id.content %}
      <a target="_blank" href="{{ fields.field_twitter_post_id.content|striptags|replace({'\n':'', '\r':'', ' ':''}) }}">
  {% endif %}
  <div class="news-type-twitter-outer">
    <div class="news-type-twitter-top-container">
      {% if row._entity.field_news_media_type[0].entity.field_fontawesome_icon.value %}
        <div class="news-type-twitter-icon">
          {{ row._entity.field_news_media_type[0].entity.field_fontawesome_icon.value|raw }}
        </div>
      {% endif %}
      <div class="news-type-twitter-name">
        EXPO2020AUS
      </div>
    </div>

    <div class="news-type-twitter-lower-container">
      {% if fields.body.content %}
        <div class="news-twitter-content">
          {{ fields.body.content }}
        </div>
      {% endif %}

      {% if fields.field_media_date_1.content %}
        <div class="news-twitter-time-ago">
          <p>
            {{ fields.field_media_date_1.content }}
          </p>
        </div>
      {% endif %}
    </div>
  </div>
  {% if fields.field_twitter_post_id.content %}
    </a>
  {% endif %}
{% else %}

  {% if fields.view_node.content %}
    <a href="{{ fields.view_node.content|striptags|replace({'\n':'', '\r':'', ' ':''}) }}">
  {% endif %}

  {% if fields.field_media.content and '<img' in fields.field_media.content|render %}
    {{ fields.field_media.content }}
  {% else %}
    <img src="/{{ base_path ~ directory }}/assets/src/images/news-texture-orange.svg" alt="news default image">
  {% endif %}


  {% if row._entity.field_news_media_type[0].entity.field_fontawesome_icon.value %}
  <div class="news-type-icon">
    {{ row._entity.field_news_media_type[0].entity.field_fontawesome_icon.value|raw }}
  </div>
  {% endif %}
  
  <div class="news-lower-container">
    {% if fields.title.content %}
      <div class="news-title">
        <p>
          {{ fields.title.content }}
        </p>
      </div>
    {% endif %}
    {% if fields.field_media_date.content %}
      <div class="news-date">
        <p>
          {{ fields.field_media_date.content }}
        </p>
      </div>
    {% endif %}
  </div>
  {% if fields.view_node.content %}
    </a>
  {% endif %}
{% endif %}

